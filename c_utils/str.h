#ifndef STR_H
#define STR_H

/**
 * Convert a string to uppercase.
 *
 * Note: the input string is converted in-place.
 * Make sure the input is a valid c-style string.
 */
void str_toupper(char *str);

#endif

